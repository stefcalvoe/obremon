name: Deploy perfumes a GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - name: Configurar Pages (lo habilita si hace falta)
        uses: actions/configure-pages@v5
        with:
          enablement: true
      - name: Subir la app
        uses: actions/upload-pages-artifact@v3
        with:
          path: perfumes
      - id: deployment
        uses: actions/deploy-pages@v4
