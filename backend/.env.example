# .env — copiar como .env y llenar con datos reales

# Supabase: Settings → Database → Connection string (URI mode)
DATABASE_URL=postgresql://postgres:PASSWORD@db.PROYECTO.supabase.co:5432/postgres

# Generar con: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=cambiar_por_clave_segura_en_produccion

# Opcional — para seed_prod.py
ADMIN_CEDULA=000000000
ADMIN_PASSWORD=obremon2025
